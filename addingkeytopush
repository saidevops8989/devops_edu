#####AUTHENTCATING REPOS WITH SSHKEYS INSTEAD OF TOKENS#######

# su -i                  #change to root user first

#ssh-keygen -t rsa                                       #this cmd generates the keys

#cat /root/.ssh/id_rsa.pub                               #cat this key and we need to copy this on github->settings->ssh&gpgkeys->newkey->givekeyname and psate they key

#ssh -T git@github.com                                   # to verify the connection

####BEFORE PUSHING WE NEED THIS CMD TO BE EXECUTED####

#git remote set-url origin git@github.com:saidevops8989/test.git

#git remote -v

#git push -u origin main
